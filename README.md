_本地前端运行_，_安全可靠_，_不上传隐私_。

# 使用方法：

1. 需要打开 设置 -> 隐私与安全性 -> App 隐私报告 -> 右上角导出。
2. 选取 .ndjson 文件 -> 选择bundleID -> 可更改导出文件名 -> 开始处理 即可。
3. 然后可点击下载 domains.txt 文件

# 结果：

下载生成的 .txt 文件，再根据自己的代理 App 添加该文件到分流规则中。

# 注意：

生成的IP中，会含有代理软件的fakeip（如 surge 的 198.18.230.183）198.18 开头为局域网地址，可以删除掉。

# 工具地址：

[tool.🍂.cc.cd/rule](https://tool.🍂.cc.cd/rule/)
